apiVersion: v1
kind: Secret
metadata:
  name: user-service-secrets
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: stock-trading
type: Opaque
stringData:
  # Replace with your environment-specific values before applying.
  db-password: "example-db-password"
  auth-access-token-secret: "example-access-token-secret"
  auth-refresh-token-secret: "example-refresh-token-secret"
  smtp-username: "example-smtp-user"
  smtp-password: "example-smtp-password"
