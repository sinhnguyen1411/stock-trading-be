apiVersion: v1
kind: Secret
metadata:
  name: user-service-secrets
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: stock-trading
type: Opaque
stringData:
  # Replace the placeholder values before applying to a cluster.
  db-password: "CHANGE_ME_DB_PASSWORD"
  auth-access-token-secret: "CHANGE_ME_ACCESS_TOKEN_SECRET"
  auth-refresh-token-secret: "CHANGE_ME_REFRESH_TOKEN_SECRET"
  smtp-username: "smtp-user"
  smtp-password: "CHANGE_ME_SMTP_PASSWORD"
